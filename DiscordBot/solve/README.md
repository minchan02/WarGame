해당 문제는 악성 DiscordBot을 활용하여 flag를 얻는 문제로
해당 DiscordBot은 역할을 부여하는 역할을 수행하고 있습니다.

그러나 secret값을 알면 높은 권한으로 승격할 수 있지만 해당 discord bot이 있는 서버에선 secret값을 얻을 수 없으므로
먼저 해당 discord bot을 내 서버로 초대해 secret값을 얻고 본 서버로 들어가 권한을 상승하면 flag를 얻을 수 있습니다
